import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles((theme) => ({
  mainBlock: {
    background: '#2D2D2D',
    width: '100%',
    height: '100vh',
  },
  progressBlock: {
    color: '#FFF',
    position: 'relative',
    top: '45%',
    left: '49%',
  },
}))
